<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_main">

    <include
        android:id="@+id/top_bar"
        layout="@layout/view_top_bar" />

    <TextView
        android:id="@+id/order_confirm_input_address"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_below="@id/top_bar"
        android:background="@color/white"
        android:clickable="true"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:lines="1"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:singleLine="true"
        android:text="@string/activity_order_confirm_input_address"
        android:textColor="@color/text_color_black"
        android:textSize="15sp" />

    <RelativeLayout
        android:id="@+id/order_confirm_pay_type_area"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_below="@id/order_confirm_input_address"
        android:layout_marginTop="10dp"
        android:background="@color/white"
        android:clickable="true">

        <TextView
            android:id="@+id/order_confirm_pay_type"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:paddingLeft="16dp"
            android:text="@string/activity_order_confirm_pay"
            android:textColor="@color/text_color_black"
            android:textSize="15sp" />

        <CheckBox
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:layout_marginRight="16dp" />

    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/order_confirm_remarks_rl"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_below="@id/order_confirm_pay_type_area"
        android:layout_marginTop="1px"
        android:background="@color/white"
        android:clickable="true">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:paddingLeft="16dp"
            android:text="@string/activity_order_confirm_remarks"
            android:textColor="@color/text_color_black"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/order_confirm_remarks"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:paddingRight="16dp"
            android:text="@string/activity_order_confirm_remarks_hint"
            android:textSize="15sp" />

    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/order_confirm_receipt_rl"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_below="@id/order_confirm_remarks_rl"
        android:layout_marginTop="1px"
        android:background="@color/white"
        android:clickable="true">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:paddingLeft="16dp"
            android:text="@string/activity_order_confirm_receipt"
            android:textColor="@color/text_color_black"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/order_confirm_receipt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:paddingRight="16dp"
            android:text="@string/activity_order_confirm_receipt_hint"
            android:textSize="15sp" />

    </RelativeLayout>

    <include
        android:id="@+id/bottom_layout"
        layout="@layout/view_order_confirm_bottom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true" />
</RelativeLayout>